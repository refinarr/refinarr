<!--
  Static favicon. Browsers cache this aggressively and it can't read
  CSS at runtime, so the colors are baked in. If you change the brand
  palette in `src/app/globals.css` (or `src/shared/theme.ts`), update
  the two fills below to match (default theme = dark-orange).
-->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" width="32" height="32">
  <rect width="32" height="32" rx="7" fill="#f59e0b"/>
  <path fill="#0a0a0a" fill-rule="evenodd" d="M9 6h8.5a5.5 5.5 0 0 1 3 10l3.5 10h-3.5l-3.2-9H12.5v9H9V6Zm3.5 3v5H17a2.5 2.5 0 0 0 0-5h-4.5Z"/>
</svg>
