services:
  remedarr:
    build: .
    container_name: remedarr
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=UTC
      - LOG_LEVEL=info
    volumes:
      - remedarr-data:/data
    ports:
      - "7272:7272"
    restart: unless-stopped

volumes:
  remedarr-data:
